/*
 * TODO: Add functions to make git commits with messages.
 * Add functions to get items from the configuration.
 * Add logic to replace a custom templating syntax. Possibly {{}}?
 *	e.g. {{file}} (first file, or only file), {{files}} (comma separated lsit of files), {{file.1}} (first file), {{file.2}} (second file),
 *	{{dirs}} (directory names where changes occured)
 */
